CKSUMF	.BYT 0          ;D-PAGE CHECKSUM
	*=$7F93+OFFSET
	JMP CONCAT
	JMP DOPEN
	JMP DCLOSE
	JMP RECORD
	JMP FORMAT
	JMP COLECT
	JMP BACKUP
	JMP COPY
	JMP APPEND
	JMP DSAVE
	JMP DLOAD
	JMP CATLOG
	JMP RENAME
	JMP SCRTCH
	JMP GETDS
	JMP OPEN
	JMP CLOSE
	JMP CHKIN
	JMP CKOUT
	JMP CLRCH
	JMP BASIN
	JMP BSOUT
	JMP LOAD
	JMP SAVE
	JMP VER
	JMP SYS
	JMP STOP
	JMP GETIN
	JMP CLALL
	JMP UDTIM
	*=$FFFA
	.WOR NMI
	.WOR START
	.WOR PULS
.END
